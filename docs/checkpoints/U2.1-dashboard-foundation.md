# U2.1 · Fundación de Dashboard

Estado: **LISTO PARA REVISIÓN**  
Base: `main@22c40bd4e1a729483aed3b4de57fa6edf787397d`

## Objetivo

Implementar la primera vista guiada por U1 usando un consumidor real que fuerce contratos reutilizables de alto retorno, sin construir una librería especulativa.

## Entregado

- contrato compartido de metadata de página en `src/applications/pageRegistry.ts`;
- demo pages de Pet Shop construidas desde esa metadata en lugar de duplicar etiqueta, ruta, icono y grupo;
- `DashboardView` como primera vista diferenciada `ADAPT` de U2;
- `DashboardDataProvider` con estados tipados `loading`, `success`, `empty` y `error`;
- mock determinista separado de la vista;
- primitivas reutilizables `MetricCard`, `StatusBadge`, `ActivityFeed` e `InlineFeedback`;
- integración del Dashboard real en `/demo/pet-shop/dashboard`;
- fallback genérico preservado para las demás páginas de Pet Shop todavía no implementadas en U2;
- Compositor/manifest actualizado a schema `0.2` con `pageKey`, `iconKey` y `group`;
- metadata compartida conservada en navegación y exportación;
- documentación viva y ejemplos reales añadidos a `/components`;
- tests de invariantes para page registry, manifest y mock del Dashboard.

## Decisiones

### Metadata de página

La identidad compartida vive en un registro técnico independiente de los presets. Una aplicación selecciona una `pageKey` y añade únicamente contexto propio, como la descripción de su demo.

El incremento no incorpora todavía las 26 páginas U1 al registro ejecutable. Solo se registran las páginas que ya existen en Pet Shop y la identidad gobernada del primer consumidor real. Las demás entrarán cuando tengan un consumidor U2.

### Datos

`DashboardView` depende de `DashboardDataProvider`. El mock es determinista y vive fuera de `pages/`, por lo que la vista no importa fixtures de dominio directamente.

### Gráficos

El resumen semanal usa barras CSS simples. No se añadió una librería de gráficos antes de tener una necesidad que justifique esa dependencia.

## Fuera de alcance confirmado

- las otras 25 vistas `ADAPT`;
- construcción masiva de primitivas del mapa U1.2;
- backend/API real;
- librería de gráficos de terceros;
- cambios a los tres shells gobernados.

## Evidencia de calidad

Durante la implementación, CI detectó dos problemas y ambos se corrigieron sin relajar las reglas del proyecto:

1. CI #105 rechazó `tone={undefined}` bajo `exactOptionalPropertyTypes`; `ActivityFeed` ahora omite la prop cuando no existe;
2. CI #107 rechazó una escritura síncrona redundante dentro de `useEffect`; `DashboardView` conserva el estado inicial de carga y actualiza solo desde la resolución asíncrona del provider.

HEAD de implementación validado:

`13ed20d11cece919b1a0be5e62f11f93458abeef`

CI #110, ejecución `35644978254`: **PASS**.

Pasaron:

- typecheck;
- lint;
- tests;
- build;
- smoke del proyecto React exportado;
- verificación del fallback SPA;
- generación del preview de PR.

La limpieza final de idioma visible y esta reconciliación documental se validan mediante un CI exacto sobre el HEAD final. Esa evidencia se registra en la PR para no volver a mover el commit después del gate.

No se afirma validación visual píxel a píxel en navegador; la restricción de navegador del entorno permanece vigente.

## Gate de cierre

- metadata compartida antes de la primera vista U2: **PASS**;
- Dashboard real consumiendo provider: **PASS**;
- estados carga/vacío/error representables: **PASS**;
- primitivas documentadas: **PASS**;
- manifest `0.2` conserva identidad de página: **PASS**;
- implementación prematura de otras vistas U2: **NO**;
- CI de implementación: **PASS**;
- CI exacto del HEAD final: **gate externo obligatorio antes de Ready for review**.

PR #11 solo puede salir de Draft si el CI del HEAD final queda verde. El merge requiere aprobación explícita del usuario.
