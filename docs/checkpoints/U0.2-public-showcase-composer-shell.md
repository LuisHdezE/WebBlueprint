# U0.2 · Public Showcase and Authenticated Composer Shell — Checkpoint

Status: **Implementation green / visual approval pending**  
Date: **2026-09-21**  
PR: **#3**  
Base `main`: `5aa62b052ec6e4b60854e67e5a6324ce4a8016ec`

## Product scope established

U0.2 establishes two explicit product surfaces.

### Public surface

- `/` public landing;
- `/apps` application catalog;
- `/apps/:slug` application presentation;
- `/demo/:slug/*` public/shareable navigable proposal demo;
- `/docs/*` public documentation entry;
- `/components` public component-library entry;
- `/login` App Composer access.

### Authenticated surface

- `/composer/*` protected App Composer workspace.

Public demos intentionally do not expose ZIP export. Export belongs to the authenticated Composer flow.

## Commercial proposal use case

A frontend demo is a first-class commercial artifact. It can be assembled for a prospective client's requested application, published under a stable direct URL, shared in a freelance-platform proposal, and later promoted into the public application catalog when useful.

The demo is technically honest: during the Blueprint/demo phase its data may be mock and backend/API behavior may not yet exist.

## Canonical publication target

WebBlueprint is intended to be published through **EliasWorks**, with the canonical domain:

- `https://eliasworks.uy`

Render is **not** a WebBlueprint production or canonical deployment target.

A temporary Render static-site preview was created during U0.2 only to obtain browser-visible visual evidence when local browser navigation was blocked. That service is not part of the product architecture, does not define the deployment roadmap, and must not be used as the basis for production hosting decisions.

## EliasWorks hosting audit

U0.2 verified the existing EliasWorks publication environment instead of guessing from the temporary preview.

Observed current architecture:

```text
LuisHdezE/erp_eliasworks
        ↓
GitHub Actions on main
        ↓
PHP 8.3 + Node build
        ↓
FTP
        ↓
public_html/
        ↓
eliasworks.uy
```

The EliasWorks root is already occupied by the portfolio application and must not be overwritten by WebBlueprint.

The hosting account also has a proven static-frontend pattern: eFactura builds its Vite WebApp and publishes the generated bundle into a dedicated directory under `public_html/`.

Apache SPA fallback is already used successfully on that hosting stack through `.htaccess`.

### U0.2 deep-link readiness

WebBlueprint now prepares for either a path or subdomain under EliasWorks without choosing one prematurely:

- `BrowserRouter` uses Vite's `BASE_URL` as its basename;
- a root/subdomain build can keep base `/`;
- a path deployment can provide a build base such as `/webblueprint/`;
- `public/.htaccess` rewrites unknown non-file/non-directory requests to `index.html`;
- routes such as `demo/pet-shop/dashboard` therefore have the required SPA fallback mechanism available for the final host.

The exact path/subdomain and CD wiring remain governed by **U0.6 · Initial CD / EliasWorks publication**. Actual deployed refresh/runtime acceptance on EliasWorks is also an U0.6 concern because no U0.2 production deployment is being introduced.

## Source-of-truth implementation

The application registry is the current source of truth for public application metadata and demo structure.

The same application definition now drives:

- public catalog/presentation metadata;
- capabilities;
- public demo pages;
- public demo navigation;
- permitted shell variant.

`Pet Shop` currently declares `vertical-light-menu`.

## Governed shell variants

Code now exposes exactly these allowed variants:

- `collapsible-menu`;
- `vertical-dark-menu`;
- `vertical-light-menu`.

All are left-side menu layouts. Horizontal menu layouts are outside WebBlueprint scope.

The allowed values are centralized in `src/shell/shell.types.ts` and validated by registry tests.

## Reusable shell architecture

`LeftAppShell` owns the reusable mechanics for:

- fixed left sidebar on desktop;
- responsive mobile drawer;
- active navigation state;
- collapsible desktop mode when the selected variant is `collapsible-menu`;
- light/dark sidebar treatment;
- sticky topbar;
- reusable footer/action slots.

Both the authenticated Composer and public application demos consume this shell rather than implementing separate sidebar systems.

`WorkspaceShell` is a Composer-specific composition over `LeftAppShell`.

## Public Style 1 foundation

Implemented:

- shared `PublicShell` with public navigation and footer;
- mobile-first public navigation;
- Style 1 landing hero;
- reusable `ApplicationPreview`;
- reusable `ApplicationCard`;
- application catalog using shared cards;
- application presentation using shared preview;
- polished login surface;
- Composer workspace using the approved left-side shell pattern.

Representative application visuals are generated from WebBlueprint UI primitives rather than external template screenshots at this stage.

## Visual V2 refinement

Explicit U0.2 feedback refined the Style 1 implementation toward:

- more efficient use of viewport space;
- smaller section/card padding;
- more contained H1 and section-heading sizes;
- compact productivity-oriented shell dimensions;
- fresh blue as the default primary/action color;
- slate reserved for text hierarchy rather than dominant CTA treatment;
- `vertical-dark-menu` retained only as an optional supported shell variant.

These rules are now recorded in the governed Style 1 baseline rather than living only in page-level styling.

## Authentication boundary

Authentication remains intentionally replaceable.

Current flow:

```text
Protected route
    ↓
Session context / hook
    ↓
Session provider
    ↓
Mock session behavior
```

Pages do not perform ad-hoc authentication checks.

A future trusted backend/service is still required if production-grade authorization must prevent unauthorized ZIP generation/download.

## CI evidence

### Initial public/auth boundary

Run `35556083250` failed because `SessionContext.tsx` exported both a component and hook under the React Fast Refresh lint rule.

Resolution: split provider, context and hook responsibilities. The lint rule was not disabled.

Run `35556178510`: **PASS**.

### Public Style 1 shell / landing

Run `35558112842`: **PASS**.

### Composer workspace shell

Run `35558225353` failed under `exactOptionalPropertyTypes` because an explicit `string | undefined` session display name was passed to a `string` optional property.

Resolution: correct the component contract. TypeScript strictness was not reduced.

Run `35558277650`: **PASS**.

### Shared LeftAppShell + governed demo shell

Run `35558418689`: **PASS**.

### Review artifact pipeline

CI uploads the built `dist` as a short-lived `web-preview` artifact on pull requests after the full quality gate passes.

Run `35558512882`: **PASS**.

Artifact:

- name: `web-preview`;
- artifact id: `10620353689`;
- digest: `sha256:d40779cb0a433d61c8c1e7c4092e428d8ce4836a682fd812c171b7a6d56a4e46`;
- retention: 3 days.

### Visual V2 density/color refinement

Run `35559663243`: **PASS**.

### Deployment-governance checkpoint before EliasWorks audit

Run `35561126203`: **PASS** at commit `deb3ae76081b365288f9e776c91353367f56957a`.

The quality gate remains:

1. `npm ci`;
2. TypeScript typecheck;
3. ESLint with zero warnings;
4. Vitest;
5. Vite production build.

## Temporary visual-preview evidence

A temporary Render static site was created only to make U0.2 browser-visible while local HTTP navigation was blocked by the execution environment.

Historical preview evidence:

- service: `webblueprint-preview`;
- service id: `srv-daoahe6gekts73bgsjng`;
- initial deploy: `dep-daoahemgekts73bgskr0`;
- initial deployed commit: `07de525f2a98a280d699177364502b070ce06d4f`.

This is **non-canonical evidence only**. No further WebBlueprint deployment architecture should be designed around Render.

## Visual-review status

U0.2 remains a **Draft PR** while the V2 visual and responsive acceptance is completed.

The project-level publication target is `eliasworks.uy`, not Render. The hosting environment has now been audited and U0.2 has the required path-portable router and SPA fallback preparation. Selecting and wiring the final EliasWorks public address belongs to U0.6 and is no longer treated as a U0.2 merge blocker.

## Remaining U0.2 gates

1. complete desktop visual acceptance of landing, applications, application detail, login, Composer and Pet Shop demo;
2. complete mobile visual/responsive acceptance of the same surfaces;
3. fix any findings without weakening the shared shell, registry or strict quality rules;
4. obtain CI PASS on the final U0.2 HEAD;
5. only then promote PR #3 from Draft to Ready for review;
6. merge only after explicit user approval.

After U0.2 closes, continue with App Composer Core and the early export flow as governed by the roadmap. EliasWorks CD remains scheduled for U0.6.
