# U0.6 · Production Acceptance

Status: **PENDING FIRST MAIN DEPLOY**

This checkpoint closes only after the deployment workflow triggered by the merged U0.6 revision succeeds in production.

Required evidence:

- `Deploy WebBlueprint to EliasWorks` workflow completes successfully;
- the deployed revision comes from the successful `main` CI artifact;
- `https://webblueprint.eliasworks.uy/` returns the WebBlueprint SPA;
- `/apps`, `/docs`, `/components` and `/demo/pet-shop/dashboard` return successfully through the production host;
- no deployment writes into the EliasWorks Laravel root.
