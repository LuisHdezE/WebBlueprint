# U2.4 · Lista operativa de pedidos

Estado: **CERRADO · LISTO PARA REVISIÓN**  
Base: `main@d2090e1553af40255ab549a5c29bb49a6b5b9cca`

## Objetivo

Convertir `orders` en la cuarta vista real de U2 y demostrar que una nueva superficie puede construirse por composición de primitivas ya extraídas, sin ampliar la librería de componentes de forma especulativa.

## Entregado

- `OrderListView` conectado a `/demo/pet-shop/orders`;
- frontera `OrdersRepository` con mock determinista e IDs estables;
- modelo UI mínimo de pedido y filtro por estado;
- búsqueda por número de pedido o cliente;
- filtro por estado operativo y combinación determinista de criterios;
- reutilización de `DataTable`, `SearchField`, `SelectField`, `StatusBadge` e `InlineFeedback`;
- estados carga, éxito, vacío y error;
- tests de determinismo, búsqueda, filtrado combinado y vacío;
- textos visibles nuevos en español;
- cero primitivas nuevas.

## Decisión de componentes

U2.4 no introduce primitivas nuevas. La vista se resolvió reutilizando los contratos ya existentes. Esta restricción deliberada valida que la extracción realizada en U2.1–U2.3 ya permite construir una nueva superficie operativa por composición.

## Fuera de alcance

- detalle de pedido;
- edición o cambio de estado;
- creación de pedidos;
- timeline de seguimiento;
- pagos;
- impresión o documentos;
- acciones masivas;
- paginación remota;
- backend/API real;
- nuevas dependencias externas.

## Evidencia

Implementación verificada en HEAD:

`e93fcd4a370c1fdcfbac819898419eea2a8e4f82`

CI #122, ejecución `35678371892`: **PASS**.

Pasaron:

- typecheck;
- lint;
- tests;
- build;
- smoke del proyecto React exportado;
- fallback SPA;
- preview de PR.

La auditoría del diff contra `main` mostró exactamente 6 archivos: este checkpoint, cuatro archivos de `src/orders/` y la integración acotada en `ApplicationDemoPage.tsx`. No se modificó ningún componente compartido, shell, export engine ni workflow.

## Criterio de cierre

- `/demo/pet-shop/orders` deja de usar el workspace genérico: **PASS**;
- la vista depende de `OrdersRepository`, no de fixtures importados: **PASS**;
- búsqueda y filtro producen resultados deterministas: **PASS**;
- no se crea ninguna primitiva nueva: **PASS**;
- typecheck, lint, tests, build, smoke de exportación y fallback SPA permanecen verdes: **PASS**.

Queda únicamente el gate final de CI sobre este HEAD documental exacto antes de marcar la PR como Ready for review.
