# U0.5 · Living Documentation / Component Gallery

Status: **READY FOR REVIEW**  
Base: `main@fd51533a7148f136c84b26e270cef3db2acc375d`

## Goal

Replace the existing documentation placeholders with useful public documentation generated around components and contracts that already exist.

## Delivered

- `/docs` is now a real living-documentation entry point;
- `/components` is now a real component gallery;
- current shared components are documented with source path, props, variants, states and usage examples;
- `ApplicationCard` and `ApplicationPreview` render live from `applicationRegistry` data;
- governed left-menu variants are previewed from `leftMenuVariants`;
- shell documentation consumes the governed variant source instead of duplicating it;
- documentation metadata is centralized and covered by invariant tests;
- existing Export Engine smoke remains green in CI.

## Boundary

U0.5 documents what exists today. Future components are documented when they enter the product rather than being speculated in advance.

## Gate

CI #69 passed typecheck, lint, tests, build, exported React project smoke and SPA fallback on the implementation HEAD. Final exact-HEAD CI must remain green after this closure reconciliation.
